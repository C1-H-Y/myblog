---
title: 03.2.10 离散化哈密顿量
categories:
- kwant
---


这一节我们介绍如何用 `kwant.continuum` 模块来离散化连续的哈密顿量。`kwant.continuum` 模块提供了一种将连续的哈密顿量转换为紧束缚模型的方法，这对于处理连续模型非常方便。

首先定义这个系统：

```python
hamiltonian = "k_x**2 * identity(2) + alpha * k_x * sigma_y"
params = dict(alpha=.5)
```

首先我们不 discretize 这系统，直接计算本征能量：

```python
h_k = kwant.continuum.lambdify(hamiltonian, locals=params)
k_cont = np.linspace(-4, 4, 201)
e_cont = [scipy.linalg.eigvalsh(h_k(k_x=ki)) for ki in k_cont]
```

然后也可以用 `kwant.continuum.discretize` 先离散化然后再去计算：

```python
template = kwant.continuum.discretize(hamiltonian, grid=a)
syst = kwant.wraparound.wraparound(template).finalized()

def h_k(k_x):
    p = dict(k_x=k_x, **params)
    return syst.hamiltonian_submatrix(params=p)

k_tb = np.linspace(-np.pi/a, np.pi/a, 201)
e_tb = [scipy.linalg.eigvalsh(h_k(k_x=a*ki)) for ki in k_tb]
```

两种方式算出来的本征值对比如下：

<div align="center">
  <img src="/images/1.4.14.png" width="600" alt="两种方式算出来的本征值对比"/>
</div>

*图 1.4.14　两种方式算出来的本征值对比*

可以看到间距越大，我们最好还是用原来的 continuous dispersion 来计算，因为我们 discretize 之后相当于有效区域就只有布里渊区 $[-\pi/a, \pi/a]$ 这么一点，能量在布里渊区边界会显著地偏离 continuous 的结果。

## 能带图示

<div align="center">
  <img src="/images/1.4.12.png" width="500" alt="能带图示"/>
</div>

*图 1.4.12　能带图示*

## 态密度图示

<div align="center">
  <img src="/images/1.4.11.png" width="500" alt="态密度图示"/>
</div>

*图 1.4.11　态密度图示*

<div align="center">
  <img src="/images/1.4.13.png" width="500" alt="态密度图示"/>
</div>

*图 1.4.13　态密度图示*
